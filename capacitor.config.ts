import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.calcprev',
  appName: 'calcprev',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
